<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link rel="stylesheet" href="/profile.css">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <!-- Navbar -->
    <!--
    <%- include("partials/navbar") %>
    -->
    <!-- Content Wrapper -->
    <div class="content-wrapper">
        <!-- Sidebar -->
        <aside class="sidebar">
            <nav>
                <a href="/home" class="nav-link">Home</a>
                <a href="/expenses" class="nav-link">Expenses</a>
                <a href="/helpfultips" class="nav-link">Helpful Tips</a>
                <a href="/settings" class="nav-link">Settings</a>
                <a href="/profile" class="nav-link active">Profile</a>
            </nav>
        </aside>
        <!-- Main Content -->
        <main class="main-content">
            <h1>Edit Profile</h1>
            
            <!-- Profile Picture -->
            <h2>Profile Picture</h2>
            <img src="/<%= profilePicture %>" alt="Profile Picture" style="width: 150px; height: 150px;">

            <form action="/profile/update-picture" method="POST">
                <label for="profilepic">Choose a new profile picture:</label>
                <select name="profilepic" id="profilepic" required>
                    <option value="browncow.png" <%= profilePicture === "browncow.png" ? "selected" : "" %>>Brown Cow</option>
                    <option value="bluecow.png" <%= profilePicture === "bluecow.png" ? "selected" : "" %>>Blue Cow</option>
                    <option value="blackcow.png" <%= profilePicture === "blackcow.png" ? "selected" : "" %>>Black Cow</option>
                    <option value="pinkcow.png" <%= profilePicture === "pinkcow.png" ? "selected" : "" %>>Pink Cow</option>
                    <option value="greencow.png" <%= profilePicture === "greencow.png" ? "selected" : "" %>>Green Cow</option>
                </select>
                <button type="submit">Update Picture</button>
            </form>
            <br><br>

            <!-- Update Email -->
            <form action="/update-email" method="POST">
                <h2>Update Email</h2>
                <div class="form-group">
                    <label for="email">New Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your new email" required>
                </div>
                <button type="submit" class="btn btn-primary">Update Email</button>
            </form>
            <br>

            <!-- Update Password -->
            <form action="/update-password" method="POST">
                <h2>Update Password</h2>
                <div class="form-group">
                    <label for="old-password">Old Password</label>
                    <input type="password" id="old-password" name="oldPassword" placeholder="Enter your old password" required>
                </div>
                <div class="form-group">
                    <label for="new-password">New Password</label>
                    <input type="password" id="new-password" name="newPassword" placeholder="Enter your new password" required>
                </div>
                <div class="form-group">
                    <label for="confirm-password">Confirm New Password</label>
                    <input type="password" id="confirm-password" name="confirmPassword" placeholder="Confirm your new password" required>
                </div>
                <button type="submit" class="btn btn-primary">Update Password</button>
            </form>
            <br>

            <!-- Delete Account -->
            <form action="/delete-account" method="POST">
                <h2>Delete Account</h2>
                <p><strong>Warning:</strong> Deleting your account is permanent and cannot be undone.</p>
                <button type="submit" class="btn btn-danger">Delete Account</button>
            </form>
        </main>
    </div>
</body>
</html>
